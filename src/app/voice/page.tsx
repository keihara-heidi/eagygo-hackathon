import { VoiceAgent } from "@/components/sidekick/voice-agent";

export const metadata = {
  title: "Sidekick Voice — streamer copilot",
};

export default function VoicePage() {
  return <VoiceAgent />;
}
